# 2026-04-09

## Accomplished

### Brand refresh integration

Holiday delivered the official brand identity package on 2026-04-08 (PDF guidelines + full logo suite). Integrated end-to-end into the website and the design vault.

**Assets**

- Mirrored the entire `HOLIDAY LOGO FILES/` directory and the brand guidelines PDF into `docs/design/assets/`
- Replaced `website/public/logo.svg` with explicit primary horizontal/vertical and standalone icon files in red and white (`logo-horizontal-{red,white}.svg`, `logo-vertical-{red,white}.svg`, `icon-{red,white}.svg`)
- Replaced favicons with the new social/web icons (32, 48, 180, 192) and updated `safari-pinned-tab.svg`
- Removed the old `logo.svg` and `favicon.png`

**Tokens (`website/src/app/globals.css`)**

- Replaced the entire color palette with the official primary palette (Holiday Red `#D00A0B`, Holiday White `#FCFCFC`, Holiday Grey `#B6B6B6`) and secondary palette (Teal `#3F786B`, Evergreen `#0A332D`, Opal `#9DBDB8`, Sand `#D6B588`, Onyx `#2C2B29`)
- Dropped all interim tokens: `brand-red`, `blue`, `taupe`, `charcoal`, `off-white`, `light-gray`, `canyon`, `river`, old `sand`, `font-stardos`
- Replaced Stardos Stencil with the `--font-alt-gothic` token (Adobe Fonts placeholder, Oswald via `next/font/google` as the Google Fonts fallback) — see Pending below
- Bumped H1/H2 to 64/44px to match the heavier display feel of Alternate Gothic in the mockup

**Components**

- `Button.tsx`: Reduced to three variants (`primary`, `outline`, `onyx`), all rounded-full pills, all with `font-alt-gothic uppercase`
- `Section.tsx`: New background options (`white`, `sand`, `opal`, `evergreen`, `onyx`)
- `Header.tsx`: White background, three-column grid, centered icon-only logo, left desktop nav, right hamburger + BOOK NOW pill
- `Nav.tsx` / `MobileNav.tsx`: Restyled red uppercase Alternate Gothic links; nav order updated to RAFTING / BIKING / ABOUT US / BLOG
- `Hero.tsx`: Simplified to image + centered headline only — no CTA, no subheading, no scroll indicator
- `Footer.tsx`: Sand background, NewsletterSignup on the left, three columns (Follow Us / Resources / Find Us), horizontal red logo bottom-left, NPS badge placeholder bottom-right
- New components: `TripCard`, `RiverSelector` (client component with crossfading background images on hover), `ContentCard` (with optional video play overlay), `NewsletterSignup`
- Deleted: `Card.tsx` (dead code), `RiverCard.tsx`, `TrustBar.tsx`, `TestimonialBlock.tsx` (no longer used after the home page rewrite)

**Home page (`website/src/app/page.tsx`)**

- Full rewrite to match the new mockup. New section order:
  1. Hero — full-bleed canyon photo + headline overlay
  2. Featured trip grid — 6 `TripCard`s + outline "View All Trips" CTA (replaces the old "Signature Trips" + "Explore by River" sections)
  3. Motor-Free Since 1966 — two-image collage + copy + outline "Learn More" CTA (replaces "The Holiday Way")
  4. `RiverSelector` — full-bleed background swap + 9-river vertical list
  5. Learn & Get Inspired — 4 `ContentCard`s + outline "Learn More" CTA
- Sanity homepage/trip/river queries are temporarily not used by the homepage (the sections render hardcoded fallback content). Sanity wiring is a follow-up — see Pending.

**Docs**

- Rewrote `docs/design/brand-guidelines.md` end-to-end against the official PDF
- Regenerated `docs/design/tailwind-tokens.md` with the new tokens and a "removed in 2026 brand refresh" mapping
- Struck the superseded "Color Additions" and Stardos guidance in `docs/design/design-upgrade-plan.md`

## Verification

- `pnpm typecheck` — ✅
- `pnpm lint` — ✅
- `pnpm build` — ✅ (3 routes generated)
- `pnpm test` — ✅ (1 file, 1 test; updated `page.test.tsx` to assert the new sections)
- Curl smoke test — ✅ all favicons, logos, and `/icon-red.svg` return 200 from the dev server; rendered HTML contains every expected `<h1>`/`<h2>`/`<h3>` from the mockup

Chrome MCP visual test was skipped — the extension was not connected during the session. Recommended to do a manual visual pass before the next deploy.

## Pending

- **Adobe Fonts kit ID** — wire the official ATF Alternate Gothic Medium kit into `app/layout.tsx`. Until then, Oswald (Google Fonts) serves as a close fallback via the `--font-alt-gothic` chain.
- **NPS Authorized Concessioner shield** — placeholder circle in the footer's bottom-right. Replace once the asset is provided.
- **Sanity schema updates** — `homepage`, `trip`, and `river` schemas need fields to drive the new sections (`featuredTrips` array, trip `category`/`startingPrice`/`description`, river `heroImage`, new `learnContent` document type). Once schemas land, replace the hardcoded fallback content in `app/page.tsx` with Sanity data.
- **Newsletter API endpoint** — `NewsletterSignup` form currently no-ops on submit. Wire to a `/api/newsletter` endpoint in a follow-up.
- **Image assets** — several home page sections reference `bikeraft.com` URLs as placeholders (motor-free collage, learn-content cards, river selector backgrounds). Replace with proper Sanity-hosted images or final brand-approved photography.
