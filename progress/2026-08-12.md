# 2026-08-12

## Accomplished — "A Day on the River" video band + site texture overhaul

Website commit `b65a5e7`, pushed to main. Continues the itinerary-media work
from [[2026-08-11]] session 2, redirected twice by Darius's design review.

### Full-bleed video band (replaces the split panel)

- `ItinerarySection` rebuilt: trip footage plays full-bleed behind the whole
  band under a `bg-evergreen/65` scrim; the accordion sits on top. Poster
  (via next/image) is the loading and reduced-motion state. No still stands
  in when there's no clip — the band is the grain texture alone.
- Scrim compared at 80/65/50% by hand. **65% is a visual judgement, not a
  verified one** — axe measures contrast against declared background colors
  and cannot sample video pixels. Needs a human pass on the opal day labels.
- **Real footage vetted and encoded.** Darius supplied
  `Deso splash video.mov` (1080p, 25.7s, one continuous shot, no burned-in
  text — unlike the feared social reels). Clean run t=8.0–15.5s cut into a
  6.5s crossfade-sealed seamless loop: 1280×720, silent, faststart, 1.8MB.
  Lives in the local demo only (`/demo-itinerary-video`, uncommitted);
  **not yet uploaded to Sanity**, so production still renders text-only.
- `AmbientVideo` bug fixed before it could ship broken: `src` was bound to
  the in-view state, so scrolling the panel out of view detached the
  attribute, unloaded the media, and re-downloaded the clip on return.
  Source now latches once; in-view only drives play/pause. Verified in
  Chromium (currentTime survives a scroll round-trip). Also gained
  RevealObserver's dead-observer failsafe.

### Texture language: line art out, material grain in

Darius rejected both wave-line attempts ("messy and inorganic"). Competitor
survey settled it — OARS runs plain white, Western River parchment grain,
AzRA canvas grain; **nobody uses vector strokes**, which at whisper opacity
only ever read as ruled lines or debris.

- New `.bg-canvas`: static `feTurbulence` grain (animated grain reads as TV
  static). Applied to the itinerary band, the contact evergreen panel,
  PageBuilder dark blocks, and every sand `Section` (review band, blog band).
- `.bg-topo` + drift animation deleted. Zero topo references left in src/.
- `TopoDivider` removed entirely (footer was the only consumer) after Darius
  cut it on sight — see bug below.

### Bugs found

1. **Footer TopoDivider was invisible to motion-enabled visitors since the
   reveal system landed.** It hardcoded `data-reveal`, but `RevealObserver`
   only observes `main [data-reveal]` and the footer renders outside
   `<main>` — so reveal CSS hid it at opacity 0 and nothing ever revealed
   it. Reduced-motion users (and every axe run, which emulates reduced
   motion) saw it fine, which masked the bug in all screenshots. Fixed by
   making reveal opt-in on the component, then mooted by deleting it.
   **Lesson for the reveal system: any `data-reveal` outside `<main>` is a
   permanent-invisibility bug the a11y suite structurally cannot catch.**
2. `AmbientVideo` src-detach reload bug, above.

## Decisions

- Full-bleed video with scrim, not a side panel (Darius call).
- Material grain, not line art, as the site texture (research-driven).
- Grain is never animated; sand cards (TripCard) stay grain-free.
- Topo motif retired site-wide, including the footer divider.

## Pending / Darius

1. **Upload the encoded loop + a poster frame to Sanity** (Deso trip,
   `itineraryMedia` field) to take the video live — the demo route
   (`src/app/demo-itinerary-video/`, local-only) shows the exact result.
   Poster should be frame 0 of the clip; Deso's only Sanity photo is the
   bluegrass band, not the canyon.
2. Manual contrast check of the 65% scrim over the brightest whitewater
   frames (opal `DAY n` labels are the weak point).
3. `Deso splash video.mov` (33MB) is sitting in the repo parent folder
   (not under git) — file it somewhere deliberate.
4. Deso/Pickpockets doc conflation in Sanity still unresolved (see
   [[2026-08-11]]).
