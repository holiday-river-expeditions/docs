# 2026-03-02

## Accomplished

- Implemented brand design tokens in the website codebase
  - Replaced default Geist fonts with Stardos Stencil (headings) and Open Sans (body) in `layout.tsx`
  - Rewrote `globals.css` with all brand colors, fonts, and typography scale via Tailwind v4 `@theme inline`
  - 7 brand colors: brand-red, taupe, charcoal, off-white, light-gray, blue, teal
  - 5-step typography scale: h1, h3, paragraph, body, link (with matching line heights)
  - Custom `tracking-tight` (-1.5px) for headings
  - Removed dark mode (not in brand guidelines)
- Updated `page.tsx` with a visual proof page demonstrating all tokens (hero, body text, CTA, color swatches)
- Created `docs/design/tailwind-tokens.md` documenting token-to-class mapping and typography recipes

## Decisions

- Used Tailwind v4 `@theme inline` for all tokens (no `tailwind.config.ts` needed)
- Font stacks match brand guidelines: Stardos Stencil falls back to Open Sans then Arial; Open Sans falls back to Arial
- No dark mode — brand guidelines only define a light palette

## What's Next

- Build site layout (header, footer, navigation)
- Start implementing page components using the design tokens
- Wire up Sanity CMS content to pages
