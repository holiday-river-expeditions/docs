---
author: claude
---

> Machine-written session log — raw technical record, not project status.

## Sanity editor guide for Justin

Wrote `reference/sanity-editor-guide.md`, a non-technical editor guide covering four asks from Darius (learning path, Holiday schema cheat sheet, publishing on the test app, image guidelines fulfilling the Aug 20 "photo guidelines and format" action item). Sources: full schema inventory from `website/src/sanity/schemas/` (12 documents + 2 blocks), actual `imageUrl()` render sizes in the app (hero 2880×1046, river selector 2880×1620, page heroes 2560×900, cards 760×740, gallery 800×600, learn cards 616×856), the revalidation webhook doc, and `reference/photo-upload-checklist.md`.

Notable choices in the guide:

- Recommended learning inside our Studio rather than Sanity Learn. Verified via web that Sanity's courses (e.g. "Day one content operations") are developer-focused, so the guide points editors at Cataract Canyon as the model trip plus sanity.io/content-editors for background only.
- Publishing section deliberately scoped to `website-phi-six-25.vercel.app` only (no beta subdomain or domain migration mentions, per Darius, to avoid confusing Justin).
- Image resolution guidance derived from the largest real crop requests: originals ≥3000 px long edge for banner slots, ~1600 px for cards, "under ~2500 px wide, find a better one" as the simple rule. Explicit warning against old-site banner strips (the low-res river images found 2026-08-12).
- Documented Studio behaviors editors hit: drafts vs Publish, Discard changes, history restore, related-trips auto-pick, ribbon fallback via `coalesce`, specialty departure exact-date matching, reserved Page slugs.

Darius confirmed `https://website-phi-six-25.vercel.app` is already in the Sanity CORS allowed list, so `reference/sanity.md` now lists it under current origins (remaining launch blockers: production domain and beta subdomain).

Doc-sync fixes alongside the new guide: corrected `reference/sanity.md` line about publishing ("changes publish immediately" → drafts save until Publish, Sanity's default flow, no review workflow on top) and added a cross-link to the new guide from `reference/photo-upload-checklist.md`.

Also published the same content as a private Claude artifact (Holiday-branded HTML, Oswald/PT Sans, brand palette from `globals.css`) so Darius can share a link before the docs site redeploys. Not committed to either repo. No website code changes.
