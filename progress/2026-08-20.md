---
author: claude
---

> Machine-written session log — raw technical record, not project status.

# 2026-08-20

## Notes restructuring after review meeting

Restructured how project notes work, per decisions Darius made after today's review meeting (AI status notes had drifted; he now owns all status communication).

- **Ownership rules codified.** Rewrote `docs/CLAUDE.md` and the root workspace `CLAUDE.md`: meeting notes, `open-decisions.md`, and task lists are human-owned; Claude writes only these `progress/` session logs. Fixed the root file's long-wrong instruction (`docs/meetings/YYYY-MM-DD-progress.md` → `docs/progress/YYYY-MM-DD.md`).
- **Progress logs demoted.** Added `progress/**` and `templates/**` to the `starlight-obsidian` ignore list in `docs-site/astro.config.mjs` — session logs no longer publish to the docs site. New format: `author: claude` frontmatter + machine-log banner (this file is the first).
- **Docs site homepage curated.** `docs-site/src/content/docs/index.mdx`: removed the "Daily progress updates" card and the GitHub-Issues reference; tagline now mentions meeting notes. Deleted stale Feb-era gitignored build artifacts (`src/content/docs/notes/`, `dist/`). Verified build: 30 pages, no progress/templates output.
- **Tracker consolidated back to the vault.** Removed the "moved to GitHub Issues" banners from `project/build-phases.md` and `project/open-decisions.md`; closed all 31 stale open issues (#7–#37, all dated 2026-02-12) on the website repo with a pointer comment. Confirmed every issue was already represented in `build-phases.md` before closing. Also fixed "Next.js 15" → 16.3 in build-phases.
- **Weekly sync scaffolding.** New template at `templates/weekly-sync.md` (agenda / notes / decisions / action items with owner+due / parking lot). Darius writes sync notes to `meetings/YYYY-MM-DD-weekly-sync.md`.

Not touched: `meetings/2026-08-12-status-meeting.md` (has Darius's uncommitted in-call notes) — he's writing today's meeting note himself.

## Docs audit and reconciliation

Audited every published vault doc against the actual website codebase and fixed the drift. 20 files changed.

- **Domain reconciled to holidayriver.com.** The April 9 meeting note recorded Lauren's purchase but it never propagated — `research/domain-name.md` still said the domain was unowned and would need acquisition, and `open-decisions.md` still had holidayriverexpeditions.com as ✅ Decided. Updated the decision, added a supersession banner to the research file, and rewrote the cutover runbook in `environments.md` (both old domains now 301 to holidayriver.com). Documented two hard blockers on retiring bikeraft.com: 7 interim photos served from it via `next.config.ts` remote patterns, and Arctic's `onlinebookingurl` pointing at its reserve flow.
- **Status inversions fixed in `build-phases.md`.** Cart/booking was documented as "not started" while `/api/book`, `/api/cart`, `MiniCart`, and `cart-cookie.ts` all shipped — corrected, with the `BOOKING_NATIVE` flag (off in prod) documented for the first time anywhere in `project/`. Conversely, beta subdomain and trip-listing filtering were ticked but aren't done — unticked. Corrected Sanity v3→v5, 9→13 schemas, 5→8-step type scale.
- **Phase 6 reality check.** Verified there is no analytics SDK, no sitemap, no robots.txt, no Open Graph, no `metadataBase`, and no JSON-LD anywhere. Recorded as the largest pre-launch gap. Accessibility got a partial tick (axe suite exists, 5 routes).
- **`architecture.md` rewritten** around an as-built route list (including the 8 API handlers and `/arctic-template`, none of which were documented), the real nav, and all 13 Sanity schemas. Proposed-but-unbuilt routes moved to their own section.
- **`github-guide.md` rewritten** — was 124 lines documenting the retired GitHub Issues workflow, and published a personal email address to the team-facing site. Now covers repos, branches, PRs, CI, and hooks.
- **Supersession banners** added to `design-review.md`, `ui-patterns.md`, `font-alternatives.md`, `page-plan.md`, and `site-audit.md`; retired `#A30D11` / Stardos Stencil / Open Sans references annotated throughout. `design-upgrade-plan.md`'s "Implementation Priority" reframed as historical.
- **Reference corrections:** `arctic-api-endpoints.md` (the flat `trippricing` endpoint 404s; token response is form-encoded), `sanity.md` (4 missing schemas, 4 missing queries, CORS launch blocker), `testing.md` + `tech-stack.md` (removed a false Lighthouse-CI claim, fixed chromium-only, reconciled the CI step list between the two files).

Docs site rebuilt clean: 30 pages, no progress logs, personal email gone.

## Docs site restyled to the 2026 brand

The docs site was still themed with the retired brand (Stardos Stencil headings, Open Sans body, `#a30d11` accent). Rebuilt `docs-site/src/styles/custom.css` and the font loads around the website's actual tokens: ATF Alternate Gothic via Typekit `guz5fen` with Oswald fallback (all-caps, heavy), PT Sans body, `#d00a0b` accents, deep-evergreen dark theme, holiday-white light theme, red current-page marker in the sidebar, and the website's branded focus ring. Verified in both themes locally and on the deployed site.

**Adobe Fonts, settled:** ATF Alternate Gothic **does load on the docs domain** via the plain `use.typekit.net/guz5fen.css` stylesheet link — verified live with a font-face probe (weight 700 `status: loaded`, headings rendering the real face). No JS embed is needed, and the website itself uses the same `<link>` in `layout.tsx`. An earlier note here claiming the kit's domain allowlist would block it was wrong.

## Docs site polish pass

- **Doubled titles fixed.** starlight-obsidian hardcodes each page's title to the file slug (no option to use the note's H1), so every page showed "BUILD-PHASES" above "Build Phases". The generated slug title is now visually hidden (kept in-flow for the `#_top` anchor and screen readers), scoped away from `.hero` so the splash homepage keeps its title. Verified every published note starts with an H1; added the one missing H1 (`meetings/2026-04-09-homepage-mockups-review.md`, formatting-only).
- **Tag pills no longer split.** `.sl-obs-tag` (Obsidian `#tags`) now renders `inline-block; white-space: nowrap`, so a pill wraps as a unit instead of breaking its background across lines. Probe confirmed 0 split pills.
- **Favicon updated to the current brand.** Replaced the Feb-era `favicon.png` with the website's icon set (32/48px PNGs, apple-touch, safari mask in holiday red), wired the same way the website does it.

## Specialty trips + trip-page jump button (meeting feedback, workstreams A & B1)

First tranche of the 2026-08-12 meeting feedback plan. Covers Justin's parent pages and jump button, and Lauren's specialty-callout field. Not touched: rates in the schedule (B2, blocked on the Arctic pricing-granularity question), open-seats treatment (C2, blocked on Lauren), redirects/SEO (D, October window), wizard (E, blocked on Lauren's questions).

- **New `specialtyType` document** (`src/sanity/schemas/specialty-type.ts`): name, slug, tagline, Portable Text description, image, `ribbonLabel`, `order`. One model serves all three of Lauren's "nav vs. wizard vs. data" axes — it flags trips, backs the `/specialty` hub the nav points at, and renders its own parent page.
- **Two new fields on `trip`.** `specialtyTypes[]` (refs) and `specialtyDepartures[]` (`{startDate, specialtyType, label, note}`). The card ribbon now derives from the family via `coalesce(ribbon, specialtyTypes[0]->ribbonLabel)` in every card projection, so the existing per-trip `ribbon`/`subtitle` keep working as overrides — Desolation renders unchanged.
- **Departure callouts.** `buildCalloutMap()` + `formatDayLabel()` in `src/lib/departures.ts`; `DepartureList` takes an optional `callouts` map keyed by start date and renders a badge inside the date cell (so it survives both the native-booking and external-link row paths). Wired into `AvailabilitySection` and `/open-seats`. Badge uses the sand fill, not red: red already means "few seats left" in this component, and a second red badge meaning "this date is special" read as urgency.
- **Join is by date, not Arctic departure id.** Editors know "Sept 12 is the bluegrass trip"; they do not know id 4417, and Arctic reissues ids each season. Cost: a date typo silently renders nothing, and same-day departures are indistinguishable — first entry wins. Desolation currently has exactly this case in live data (two "May 31 – Jun 4" departures, one Full).
- **Routes.** `/specialty` (static, 1m ISR) groups every family with its trips, alternating white/sand grounds; `/specialty/[slug]` is banner + body + an "Upcoming Dates" list built from authored dates (no Arctic call — live seats stay one click away via `/trips/[slug]#dates-and-rates`) + the family's trip grid. Added `specialty` to `RESERVED_SLUGS`.
- **Nav.** Added Specialty as a fifth item in `Nav.tsx` (mobile drawer inherits it). Also updated the duplicated `NAV` const in `src/app/arctic-template/route.ts` — the architecture doc's two-places warning was accurate and would have drifted.
- **B1 jump button.** `AvailabilitySection` exports `AVAILABILITY_ANCHOR = 'dates-and-rates'` and carries `id` + `scroll-mt-28`; the trip-page fact bar gained an outline anchor next to "Book Now". Labelled "Dates & Availability" to match the heading it lands on — it becomes "Dates & Rates" when B2 adds prices.
- **Seed.** `scripts/seed-specialty-types.mjs` creates the five families from `project/site-audit.md` and links Desolation to Canyon Concerts (`setIfMissing`, so Studio curation is never clobbered). Deliberately authors no `specialtyDepartures` — those name real dates on real departures and are Holiday's to write.

Verification: 50 unit tests pass (13 new — 7 on `buildCalloutMap`, 3 on `formatDayLabel` incl. the UTC day-slip case, 4 component tests proving the badge renders, links to the parent page, hits only the named date, and does not displace the seats badge). 11 Playwright tests pass with `/specialty` and `/specialty/canyon-concerts` added to the axe scan. Clean typecheck, lint, and production build. Desktop nav at the 1024px breakpoint fits with 112px headroom; no horizontal overflow at 375px. Arctic-down check via `next start` against an unreachable host: trip pages and `/open-seats` return 200 with the phone fallback, both specialty routes 200 (they take no Arctic dependency by design).

Not verified: the interactive Studio round-trip — the Studio compiled with no schema error and `sanity schema extract` succeeded, but the local Studio sits behind a login and authenticating into the Sanity account is not mine to do.
