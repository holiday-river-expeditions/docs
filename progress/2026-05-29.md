# 2026-05-29

## Accomplished

### Homepage alignment with the Figma mockup

Reviewed a new Figma homepage mockup (and a refined follow-up) against the live build and brought the implementation into fidelity with it.

**Audit result:** the design tokens were already correct — every brand color in `globals.css` matches [[brand-guidelines]] to the hex, the type scale matches [[tailwind-tokens]], and the fonts (ATF Alternate Gothic + Oswald fallback + Open Sans) are wired right. Section structure already matched. So the work was fidelity cleanup + the mockup's new elements, not a rebuild.

**Changes made:**

- **Header** — swapped the icon-only mark for the horizontal logo lockup (`/logo-horizontal-red.svg`) in `Header.tsx`, matching the mockup and the brand default.
- **Hero** — switched from viewport-relative height to a fixed ~550px (`Hero.tsx`); added the red **"60 Years of Going With the Flow"** seal, straddling the bottom-left edge (awaiting the asset file).
- **Featured trips** — added a specialty `TripCard` variant (`ribbon` / `subtitle` / `featured` props → red ribbon, red border frame, red subtitle). Replaced "The White Rim" in the grid with the **Desolation Canyon — With The Pickpockets Bluegrass** music trip (placeholder image/price/copy).
- **"Rafting Since 1966"** — dropped "Motor-Free" from the section heading (per the refined mockup), stacked it on three lines, and changed the body copy to exclamation marks.
- **Founder signature** — deferred. The hand-lettered "Dee Holladay" signature + arrow couldn't be exported from Figma yet (no design access), so it's omitted for now; the exact `<Image>` snippet is preserved as a comment in `page.tsx` for a one-line restore once the asset lands. (Declined a generic-font stand-in — it's a real founder signature.)
- **Footer** — replaced the NPS badge placeholder with a real `<Image>` for the **"Authorized Concessioner"** arrowhead.
- **Docs** — updated [[design-upgrade-plan]] (its section/component plans were stale — described Trust Bar, Testimonials, teal accents that were never built).

## Decisions

- **Section heading drops "Motor-Free"** → now just "Rafting Since 1966", per the refined mockup. (Motor-free remains a core brand differentiator elsewhere.)
- **Dee Holladay signature will use the real hand-lettered asset** (arrow included), not a font stand-in. An interim code-built version (Caveat + SVG arrow) was prototyped then removed; the signature is omitted until the asset can be exported.
- **60-years seal + NPS badge** added to `public/` and rendering; wired via `<Image>`.
- Specialty trip is a **reusable `TripCard` variant**, not a one-off component.

## Verification

- `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅ (1/1; updated the section-heading assertion to be whitespace-tolerant for the `<br/>`-stacked heading)
- Rendered the homepage in the dev server and screenshot-compared against the mockup: header lockup, specialty card, "Rafting Since 1966" heading + copy, Dee Holladay signature, river selector, and footer all match. The 60-years seal and NPS badge render as their (pending) image slots.

### Figma-API typography & fidelity pass (afternoon)

Got a read-only Figma personal access token and pulled the **exact** specs from the refined artboard (frame "04", node `1:1098`) via the REST API + `/images` renders — replacing the earlier eyeballing of flattened PNGs. Confirmed 04 is the right target (it's the only frame with the 60-years seal, the Desolation specialty card, "RAFTING SINCE 1966" without "Motor-Free", and the Dee Holladay signature; frames 01–03 are older).

**Corrected from exact API values:**

- **Trip prices** (digits weren't legible in the export): Cataract `$1,830 → $1,630`, Westwater `$595 → $599`, Gates of Lodore `$1,385 → $1,300`, Yampa `$1,385 → $1,300`. (Maze `$1,415`, Desolation `$1,300` already correct.)
- **Category tag** is a **solid fill** pill (teal `#3F786B` rafting / sand `#D6B588` biking, white text) below the image — was a red-outline pill. Now color-coded by category.
- **Trip card image** aspect → `362/355` (near-square) — was `4/3`.
- **Specialty card**: ribbon moved to **top-left** (was centered), red frame `2px → 4px`, subtitle bumped to 22px/800, body copy removed (mockup leads with the subtitle).
- **Maze description**: dropped "the" to match mockup.

**Typography sweep (weights/sizes now match frame 04):**

- **Body face → PT Sans** (400/700), replacing Open Sans (see [[tailwind-tokens]] / [[brand-guidelines]]). Loaded via `next/font/google` in `layout.tsx`.
- **Heading weight → 900 (`font-black`)**: hero h1, "Rafting Since 1966", trip card titles, specialty subtitle, river-selector names, Learn card titles, "Learn & Get Inspired". Prices/footer links → 600; footer column headers → 400; nav/tags/buttons/newsletter headline → 500. Oswald fallback widened to 500/600/700.
  - _Weight note:_ first set these to 800 (what the Figma API reports), but the client said they still didn't look as heavy as the mock. Verified in-browser that the build was correctly rendering ATF Alternate Gothic 800 (font loaded, computed weight 800) — so it wasn't a fallback bug. Desktop→web-kit weight numbering differs; the kit's **900** visually matches the mock, so headings are now `font-black`. Kit `guz5fen` publishes all weights 100–900.
- **PT Sans wasn't actually applying** (latent bug): `globals.css` had `body { font-family: var(--font-sans) }`, but `@theme inline` never emits `--font-sans` as a `:root` custom property, so it resolved to empty and body text fell back to `ui-sans-serif` (was true for the old Open Sans too — unnoticed because macOS `ui-sans-serif` looks clean). Fixed by setting the body font via `var(--font-pt-sans)` directly. Confirmed body/descriptions/section paragraph now compute to PT Sans with the 400/700 faces loaded.
- **Section heading sizes**: "Rafting Since 1966" → 64px (was 44); "Learn & Get Inspired" + newsletter headline → 36px; section body paragraph → 20px PT Sans; card descriptions → 14px.
- **River selector** names → 56px desktop (was 64), extrabold. **Nav links** → 18px, no letter-spacing.

**Hero → inset banner**: image now sits below the header with side margins, fixed 523px desktop height (per mockup), instead of full-bleed. The 60-years seal straddles the banner's bottom-left edge.

**Footer background color fixed**: was `bg-sand` (`#D6B588`) — mockup is cream **`#F3F0EB`**. Footer column headers/links re-weighted (400 headers, 600 links).

**Discovery — Dee Holladay signature**: the API shows it's **live text in the "Bright Topic" font** (bold, ~32px, black) plus a **separate arrow image**. So only the arrow needs exporting; the name can be set in the Bright Topic webfont (commercial — needs a license/kit) once we decide to wire it.

Verified: `pnpm typecheck` ✅, `pnpm lint` ✅, `pnpm test` ✅. Couldn't start a parallel preview (Next 16 locks one dev server per project dir; the local one was running) — changes hot-reload into the running server.

### Homepage wired to Sanity CMS (content management)

Moved the homepage off hardcoded arrays onto Sanity so the Holiday team manages it in `/studio`. The dataset (`jau3o5v4` / `production`) was empty and the `homepage` schema still described the old design — fixed both.

- **Schemas realigned to the real design.** Rewrote `homepage` (hero, ordered `featuredTrips` references, "Rafting Since 1966" story block, ordered `rivers` references, `learnContent` array) with field groups for editor clarity. Added trip-card fields to `trip`: `tagline`, `startingPrice`, `durationLabel`, `subtitle`, `ribbon`; removed the old `featured` boolean (homepage now curates via a reference list — single source of truth, drag-to-reorder).
- **Queries + types.** Rewrote `homepageQuery` to dereference trips/rivers/learn cards; removed `featuredTripsQuery`/`getFeaturedTrips`. Regenerated `src/sanity/types.ts` via `pnpm typegen`.
- **`page.tsx` is now an async server component** fetching `getHomepage()` and mapping to the components, with `urlFor()` for images and graceful empty-image handling (neutral placeholders; evergreen base on hero + river selector so white text stays legible until photos land).
- **Seeded `production`** via `scripts/seed-homepage.mjs` (idempotent, deterministic `_id`s): 2 categories, 9 rivers, 6 trips, the Homepage singleton + Learn cards. **Text/structure only — no images** (team supplies real photos).
- Verified: GROQ reads back all sections in order; homepage renders 200 with all Sanity content; `typecheck` / `lint` / `test` green (test now mocks the Sanity layer and `await`s the async page).

**How the Holiday team adds images:** in `/studio` → **Homepage** (hero image, the two story photos, each Learn card image), **Trips** (photos — first photo is the card image), **Rivers** (image). Uploads appear immediately via `cdn.sanity.io` (already allow-listed in `next.config`).

## Pending

- **Dee Holladay signature** — ✅ implemented. The name uses the commercial **"The Bright Topic"** font (Letterhend Studio; not on Adobe Fonts), so rather than license/self-host it, the name + arrow were exported from Figma as **two separate SVGs** (`dee-holiday-signature.svg`, `hand-drawn-arrow.svg`) and composed in code. The portrait cell is now `relative` with the photo in an inner clip wrapper; the arrow + signature are absolutely positioned over its bottom-right corner (`z-10`) so they stay anchored to the portrait responsively. Offsets are approximate — fine-tune the `bottom/right/w-[…]` classes.
  - _Still needs:_ the **portrait photo** itself — `motorFreeImages.right` points at a dead `bikeraft.com/.../2021/Dee-Holladay-portrait.jpg` (404), so the signature currently sits over an empty frame until a real portrait is wired.
- **Specialty card content:** real bluegrass band photo (price/duration now verified: `$1,300` · 4/5 days).
- **Trip prices/durations** — ✅ now verified against the Figma API and corrected.
- **Placeholder image 404s:** the Motor-Free collage (`/2021/` URLs) and some trip/learn images point at dead `bikeraft.com` URLs — replace with real assets / Sanity content.
