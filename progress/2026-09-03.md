---
author: claude
---

> Machine-written session log — raw technical record, not project status.

## Homepage Map tab (PR #72) and deploy verification

Context: the deploy and contract phase for the taxonomy merge (PR #71) happened earlier today and is recorded in `progress/2026-08-28.md` (that file was opened on a date notice that turned out to be stale; commit timestamps put all of it on 2026-09-03).

**Editor guide pass.** Read `reference/sanity-editor-guide.md` end to end after the taxonomy edits and found four stale spots: the sidebar step said "Trip →" (entry is Trips), the Related Trips fallback said "same river or activity", the photo-size note referenced the retired river selector, and the Homepage row described a River Selector tab as if the carousel still existed. Fixed and pushed (`b166185`); the earlier uncommitted audit append to `progress/2026-08-27.md` was committed and pushed alongside (`12faeae`). Verified the published page at `docs-umber-seven.vercel.app/notes/reference/sanity-editor-guide/` carries the changes and no `Activity`/`Trip Category` rows. Route note: vault folders publish under `/notes/<folder>/<slug>/`.

**PR #72** (`777643c`, fast-forward): `homepage.ts` group `rivers` retitled River Selector → **Map**, field Rivers → **Map Pins**, help text now says the list feeds map pins, order is irrelevant, and a section only appears once its coordinates exist in `lib/trip-map-data.ts`. Four-line diff, schema labels only. CI green; merged via `gh pr merge --merge --delete-branch`. Guide's Homepage row updated to match (`ad0cc21`).

**Verifying the deployed Studio bundle** took several wrong turns worth recording:
- Comparing asset lists between the production alias and the deployment's own URL is invalid on this project: the deployment URL 302s to Vercel SSO, so the "mismatch" was against a login page. GitHub's Production deployment record (`777643c`, success) is the reliable signal.
- Static crawling of `/studio` HTML and its chunks (with both `/_next/static/...` and Turbopack's relative `static/immutable/chunks/...` patterns, three levels deep) never reaches the Studio's lazily loaded schema chunk; the RSC flight payload doesn't name `NextStudio` either. Not a usable test on Next 16 + Turbopack.
- The in-app browser's network log after hydration listed only ~32 scripts / 176 KB, which cannot be the multi-MB Studio, so it was incomplete too.
- What worked: `performance.getEntriesByType('resource')` inside the loaded Studio page, then fetching each script in-page and searching its text. 34 scripts, 6.3 MB. `Map Pins` and the new help text are in `2hkwofeaylw2s.js`, the same chunk as the control string `Rafting Since 1966`; `River Selector` is absent from all 34. The Studio bundle loads before its login wall, so this needs no Sanity credentials.
- Two shell gotchas along the way: in zsh, a loop variable named `path` clobbers `PATH` for that command; and grep on minified chunks needs `LC_ALL=C` / `grep -a` or it fails with "character not in range".

**Training outline** for the Sanity session was delivered in chat, not written to the vault (Darius's session, his notes).
