---
author: claude
---

> Machine-written session log — raw technical record, not project status.

## Anniversary seal: "60+" instead of a live number (branch `feature/remaining-feedback`, still uncommitted)

Darius: the agreed treatment is a plus once the count passes 60, not a raw integer. `AnniversaryBadge` no longer swaps the numerals for text; the original "6" and "0" paths always render, and new `anniversaryLabel(years)` returns `'60'` through 2026 and `'60+'` after, which drives both the aria-label and a 34px Alternate Gothic plus at the top-right of the zero (x 128, y 60, the seal's −4.05° tilt). Bounding-box check of the 34 artwork paths: the only path with x > 105 and y < 70 is the zero itself, so that corner is clear of the ring lettering. Verified the placement against the real Typekit face in the browser via a throwaway `public/badge-preview.html` (deleted afterwards). Badge unit tests rewritten for the label and both render states; 158 unit tests green. Docs synced: `architecture.md`, `demo-panel-guide.md`, `build-phases.md`.

Also restarted the dev server on :3000 for Darius — it had died with the browser pane at the end of the 09-10 session.

## Trip finder: "Close" on a full answer set

Darius answered every question and got "Close — this is what guides are for". Probed all 375 full-answer combinations against the live data (`curl` of `/trip-finder?…`, h1 extracted): the only combinations that land on "Close" are the 75 with `age=u5` — every trip has a minimum age of 7+, so the best match carries an `ageConflict` and can never be confident. Correct ranking, wrong words: the copy said "tell us one or two more things" to someone who had answered everything.

- New `shortfallReason()` in `lib/trip-finder.ts` (`'age' | 'thin' | 'low'`). Results copy now names the reason: age → "The littlest one needs a few more birthdays" + a line saying every trip has a minimum age and the nearest is shown with its rule (the existing caveat under the reasons carries the specific age); thin (<2 answers) → the go-to trip or, without one, the old "tell us more"; low (full answers, weak score) → "nothing lines up on every count, this is the nearest fit".
- `selectMatches()` now applies the go-to trip only for the thin case. A full answer set that scores low keeps the scored order — leading with Westwater over a visitor's actual answers would be a default dressed as advice.
- Tests: two new scorer tests (age conflict → `'age'`, no fallback; biker in October → `'low'`, scored order kept). 160 unit tests.
- The Studio's Trip Finder document still has no Go-to trip reference. A targeted Sanity patch to set it was blocked by the tool sandbox, so it is a one-click Studio edit for Darius: Trip Finder → Tuning → Go-to trip → Westwater Canyon. Until then thin answer sets lead with the phone line, as before.

## Darius's answers on the leftover list (2026-09-12)

Recorded here because they change code and content; the decisions themselves are his to log.

- Specialty-as-one-card: pushed back, it works against the taxonomy; a filter is the likelier answer later. Not building.
- Getting Here / Before You Go standalone pages: wait.
- Go-to trip: he set it to **Gates of Lodore** in the Studio (verified: `fallbackTrip` → `trip-gates-of-lodore`). Code default in `DEFAULT_TUNING` changed from `westwater-canyon` to `gates-of-lodore` to match; the seed script now keeps an existing Studio reference instead of overwriting it; docs updated (trip-finder, editor guide, architecture, build-phases).
- Justin's content errors: on inspection with the raw perspective, the "second San Juan section" was `drafts.river-san-juan` — an unpublished draft renaming the section to "San Juan River" — not a second document. The combo section (`dbc15205-…`, "Moab Biking / Westwater Rafting Combo", created 2026-08-27) was a published document with zero references. Both removed with a one-off `@sanity/client` transaction (scratchpad, not committed): draft discarded, combo section deleted. Sanity keeps document history, so either is recoverable from the Studio's history view if needed.
- Hilary's map: in progress on their side. Mobile item = a mobile audit plus a change to the floating menu (details TBD). Wizard stays single-choice. Publish workflow: fixed.

## Inner-page banners match the homepage hero (2026-09-12)

Darius: the trip-page hero is now as tall as home, and every other page should get the same treatment. The same banner markup was copy-pasted in five places (trip page, section page, `TripTypeLanding`, blog post, `PageBuilder`'s hero block) at `h-[320px] md:h-[440px]`. Extracted `components/ui/PageBanner.tsx` (hero proportions: `h-[460px] md:aspect-[1440/523] md:max-h-[75vh]`, bottom-weighted scrim, children slot for eyebrow/heading/subline, `priority` prop for non-first hero blocks) and exported `PAGE_BANNER_WIDTH/HEIGHT` = 2880×1046 so the Sanity crop matches the display aspect at 2x (was 2560×900). All five call sites swapped; `next/image` imports dropped where nothing else used them. `/trips` keeps its flat evergreen title bar (no image to show). Typecheck, lint, Prettier, 160 unit tests green; verified /rivers/westwater, /rafting, /about, blog post in the browser at 1280 wide. Editor-guide banner size guidance (≥3000px long edge) still holds.

## Pre-commit review pass (2026-09-12)

Darius asked for one more pass before committing. Ran `/code-review high` over the working tree (eight finder angles, one-vote verify) plus the full Playwright suite (24 green) and a manual walk. Ten findings survived; all fixed:

- **Carousel** (`Carousel.tsx`): the track was not positioned, so `offsetLeft` was body-relative and Next/Previous overshot by the page gutter on wide viewports (slide 2 unreachable at ≥2072px). Track is now `relative`; the scroll handler batches its layout reads per animation frame; the track is `tabIndex=0` with a focus ring so keyboard users can scroll it (axe `scrollable-region-focusable`, which would have failed the a11y e2e the moment a trip had two photos).
- **ReviewsSection**: rendered "What Guests Say" over an empty band on the five trips with no quotes. Heading and carousel now gate on quotes; without them it collapses to the compact rating + links strip the old `TrustStrip` gave.
- **TripFinderLogicPanel**: still derived "shown" from `ranking.slice(0, resultsShown)` and said "leads with call-us" whenever the go-to trip led. Now takes `matches` + `fallbackApplied` from the page and explains all four outcomes (go-to, confident, age conflict, close fit).
- **Trip map on pin-only sections** (`/trips/the-maze`): `emphasize` matched no stretch, so every other river faded to 0.35 under a red key swatch labelled "Maze". The map now mounts only when the section has drawn geometry or authored photo points; emphasis and the swatch only apply when a stretch exists.
- **Scripts**: `new URL('.', import.meta.url).pathname` → `fileURLToPath` in the worker-copy and stretch-fetch scripts (a space in the checkout path would have silently broken the worker copy).
- **`next/image` `priority`** is deprecated in Next 16 (`node_modules/next/dist/docs/.../image.md:293`); `PageBanner` and the hero block now use `preload`.
- **AnniversaryBadge** inlined ~20 KB of path data into the HTML and the RSC payload of every page. Now a `<use href="/badge-60-years.svg#seal">` against the static file (wrapped its artwork in `<g id="seal">`), so the paths stay one cached request; verified rendering in the browser.
- **Reuse**: extracted `useHoverCard` (both maps), `useInView` (both lazy-mount sections), `floating-bar.ts` (both bars' position/docking/chrome), `TripCatalogDisclosure` (specialty + landings, summary now via `buttonClasses` so it matches every other outline CTA), `contactPhone`/`contactEmail` (homepage, contact page, footer), and the homepage hero now uses `PAGE_BANNER_WIDTH/HEIGHT`. Dropped the dead `stretches` prop on `MapLayers`.
- **Simplification**: results copy is one `REVEAL[outcome]` lookup instead of a nested ternary plus four guarded blocks; `selectMatches` checks `shortfallReason !== 'thin'` alone; reviews and map points are `filter`/`map`; the map section element is built once so the grid class and the slot can't disagree.
- **Conventions**: legend swatches use Tailwind token classes (`RIVER_COLORS` now carries `{hex, swatch}`) instead of inline `style`; geopoint help text no longer promises a map picker the Studio doesn't have; `seed-pages.mjs` seeds the new email.

Left as-is, with reasons: `/rafting` and `/biking` fetch the whole catalog for the collapsed disclosure (ISR 60s, seven trips — negligible); `stretchBySlug` pulls the 73 KB geometry into the trip route's *server* bundle (never the client); `configureMapLibreWorker()` stays a per-map-component call (only two exist, and `map-style.ts` is also imported server-side where `maplibre-gl` must not load).

Final state: typecheck, lint, Prettier, 160 unit tests, 24 e2e (the a11y sweep counts one test per route) all green. Ready to commit.
